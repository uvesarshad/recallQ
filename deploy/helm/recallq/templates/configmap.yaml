apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "recallq.fullname" . }}-env
  labels:
    {{- include "recallq.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
