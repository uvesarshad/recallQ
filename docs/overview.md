# Recall Project Overview

> Scope: High-level vision, tech stack, glossary, and index of Recall documentation.
> Rendering context: N/A
> Project tier: 4
> Last updated: 2026-07-14

## Overview
Recall (product name: RecallQ, by Montr AI) is a frictionless capture and intelligent organization tool designed to make high-density personal knowledge searchable and navigable. It captures notes, URLs, and files from any surface (web, Chrome extension, iOS/Android, Telegram, email) and enriches them using Gemini AI. Users interact with their archive via search, LLM-based RAG chat, and a freeform canvas. Billing is handled via Razorpay (web/extension/Android — iOS is free-only for v1).

## Project Tier and Tech Stack
Recall is classified as a Tier 4 production SaaS. The codebase is a pnpm + Turborepo monorepo with three apps — the Next.js web app under `apps/web/`, the WXT Chrome extension under `apps/extension/`, and the Expo iOS/Android app under `apps/mobile/`. Shared TypeScript packages live under `packages/` (`@recall/api-schema` for Zod schemas, `@recall/api-client` for the typed REST client used by non-web clients).

The technical stack consists of:
- Monorepo: pnpm workspaces + Turborepo. Single workspace-root `.env` shared by web, workers, and DB scripts.
- Web framework: Next.js (App Router, Turbopack) with a dark-first UI theme.
- Runtime & Language: Node.js with strict TypeScript and direct TSX-based worker execution.
- Styling: Tailwind CSS v4 and custom design tokens in the global stylesheet.
- State Management: React local state and context-based state propagation with no third-party store.
- Database: Raw PostgreSQL Pool client via the pg package, coupled with pgvector for semantic similarities.
- Authentication: NextAuth v5 (beta) with PostgresAdapter for the web cookie session; bearer-token auth (personal access tokens) for the extension and mobile, per Stage 1 of PLAN.md.
- Payments: Razorpay API integration with webhook listeners to manage subscription tiers. iOS billing deferred (no Apple IAP for v1).
- AI Services: Gemini API via the Google Generative AI SDK, using gemini-2.5-flash-lite for summary/tags and text-embedding-004 for vector embeddings.
- Background Processing: Discrete daemon tasks (enrichment, reminders, generic jobs) under `apps/web/workers/`, supervised by systemd, PM2, Docker restart policies, or an orchestrator in production.
- Chrome extension: WXT 0.20 + React + Manifest V3. Popup, background context menu, `chrome.identity.launchWebAuthFlow` sign-in.
- Mobile: Expo SDK 52 + React Native 0.76 + Expo Router 4 + NativeWind 4 + Expo SecureStore. Expo Go supports sign-in, feed, item detail, capture, and SQLite offline queue flows; push and native link parity require a development build.

## Directory Map
- docs/overview.md (This file): Entry index, technology overview, directory map, glossary, and changes log.
- docs/architecture/folder-structure.md: Top-level layout and module grouping, naming rules, and convention boundaries.
- docs/architecture/rendering-strategy.md: Next.js rendering modes, static routes, dynamic handlers, and auth checks.
- docs/architecture/data-flow.md: Step-by-step lifecycles of capture, AI enrichment, vector mapping, and RAG retrieval.
- docs/ui/component-library.md: Detailed layout of shared shell, dialogs, cards, and interactive visualization maps.
- docs/ui/layout-system.md: Shared layouts, nested views, PWA setup, and user navigation panels.
- docs/ui/theming.md: Colors, font scales, border radiuses, and spacing constants.
- docs/api/route-handlers.md: Mapping and contracts for items, actions, chat, email, graph, me, reminders, and webhook routes.
- docs/api/sdk-client.md: TypeScript client, CLI, and generated API contract examples.
- docs/api/mcp-server.md: Local stdio MCP bridge for bearer-authenticated archive tools.
- docs/api/server-actions.md: Sign-in and auth-handling server actions.
- docs/api/external-services.md: Third-party integrations (Gemini, Resend, Razorpay, Telegram) and format extraction engines.
- docs/api/database.md: PostgreSQL tables, vector dimensions, indexes, and initial-to-billing schema migrations.
- docs/state/client-state.md: Browser local states, custom events, and filter state management.
- docs/state/server-state.md: Fetch-based state hydration, client cache sync, and optimistic archive mutations.
- docs/auth/auth-flow.md: Provider setups, JWT callback mapping, and development login bypass.
- docs/auth/authorization.md: Layout guards, plan limit enforcement, and header token ingest validation.
- docs/infra/environment.md: Environment schema Zod validation list.
- docs/infra/deployment.md: Local execution, production builds, and daemon background execution.
- docs/infra/self-host-docker.md: Docker Compose self-host package for web, workers, PostgreSQL/pgvector, and file-storage volumes.
- docs/infra/kubernetes-helm.md: Minimal Kubernetes/Helm package for web, workers, ingress, and archive storage.
- docs/infra/testing.md: CLI test run scripts, utility coverages, and custom tests map.
- docs/openapi.json: Generated OpenAPI 3.1 API artifact produced by `pnpm openapi:generate`.
- docs/security-audit.md: Per-endpoint audit of `/api/v1/*` confirming auth, validation, plan-limit, and rate-limit guards.
- docs/performance-audit.md: Database hot-path inventory, index coverage, bundle composition, worker cost, and operational growth concerns.
- docs/plan/2026-07-07-app-performance-security-feature-audit.md: Current RecallQ audit covering performance, security, operational reliability, and feature enhancement priorities.
- docs/plan/2026-07-07-karakeep-comparison-audit.md: Competitive audit comparing RecallQ with Karakeep and identifying product, architecture, feature, performance, and documentation gaps.
- docs/plan/2026-07-07-karakeep-audit-task-list.md: Execution backlog generated from the Karakeep comparison audit, with priorities, statuses, and acceptance checks.
- docs/plan/2026-07-07-i18n-plan.md: Internationalization rollout plan for post-core stabilization.
- docs/plan/2026-07-14-uiux-motion-audit.md: UI/UX motion and animation audit of the web app and Chrome extension — easing/duration, physicality, interruptibility, performance, accessibility, cohesion/tokens, and missed-motion opportunities, with a prioritized backlog.
- docs/plan/2026-07-14-uiux-motion-audit-task-list.md: Execution backlog generated from the UI/UX motion audit, with P0-P3 priorities, statuses, and per-task acceptance checks.
- docs/modules/capture.md: Deep dive into the multi-surface capture engine, action extraction, and limits checking.
- docs/modules/search-chat-canvas.md: Deep dive into hybrid search (now rendered inline in the Feed), streaming RAG chat, and the freeform infinite canvas.
- docs/modules/billing-settings.md: Deep dive into plans, settings routing, and webhook-driven subscription sync.
- docs/modules/extension.md: Deep dive into the Chrome extension — context-menu tab management, the pinned-tab app, capture, and plan-gated settings sync.

## Glossary
- Archive Item: The core unit of knowledge (URL, note, plain text, or file) captured by the user.
- Ingestion: The entry pipeline that validates, performs command parsing, limits check, and saves the item.
- Enrichment: The async post-save AI pipeline that scrapes, parses, summarizes, tags, and embeds the item.
- RAG: Retrieval-Augmented Generation, used to answer queries in the chat panel based on the semantic search of context items.
- Item Relation: AI-inferred similarities (embedding cosine distance) or manual links between captured items.

## Recent Changes
- [2026-07-14] Motion audit execution — polish wave (task-list P1.1, P1.3, P1.5, P2.1, P2.3, P2.4, P3.2, P3.3, P3.4; P0.3 partial). Fanned out across seven file-disjoint subagents plus a hand-done canvas rewrite. Canvas pan (`canvas-client.tsx`) rewritten to move the card layer via `transform: translate3d()` written through refs (no `setPos` per mousemove; `left/top` removed) — kills the per-frame React re-render + layout/paint. Added `scale(0.97)` press feedback across pills (`packages/ui/Pill.tsx`), the feed card + action buttons, and both extension button rules. Replaced all six `transition: all` in settings with explicit token'd property lists. Reworked `Tooltip` to an always-mounted, interruptible opacity/transform transition with trigger-anchored origin. Added `AnimatePresence` enter/exit to `ItemDetailModal` + `CreateItemDialog` (was teleport-close), chat bubble/citation entrances in `ChatDock` (`initial={false}` so streaming doesn't re-animate), and CSS-only extension motion (row entrance, Synced/Pending badge cross-fade, popup save-confirmation) with a reduced-motion block. Added `prefers-reduced-transparency`/`prefers-contrast` fallbacks and `no-touch-lift` hover gating; removed the dead `pulse` keyframe reference; reduced Atmosphere blob blur 80px→40px. Deferred (deliberate, logged): feed masonry layout animation (P3.1, wants visual iteration), per-card blur-on-scroll (P0.3, needs profiling), framer transform-string micro-opt (P1.4). Typecheck + web build + extension build all green. Updated `docs/ui/theming.md` and `docs/modules/extension.md`.
- [2026-07-14] Motion audit execution — foundation wave (task-list P0.1, P0.2, plus P1.2, P1.6, and the focus half of P2.2). Added a motion token layer to `@recall/tokens`: `MOTION` (`duration.fast/base`, `ease.out/inOut`) and framer-motion spring presets `SPRING_UI` (critically damped) / `SPRING_POP`, mirrored as `--duration-*`/`--ease-*` CSS vars in `app/globals.css` and `packages/tokens/src/tokens.css` (kept in `:root`, not `@theme`, to avoid Tailwind utility collisions). Deduped the `float` keyframe (removed ChatDock's inline `FLOAT_KEYFRAMES`), converged all five hand-typed spring configs (ChatDock ×2, CaptureBar, ItemCard, FloatingMenu) onto `SPRING_UI`, and tokenized their transition strings (incl. ItemCard's 500ms preview-image transition → 0.2s `ease-out`). Rebuilt reduced-motion: the `globals.css` blanket no longer clamps `transition-duration` (opacity/color/theme-change fades survive) while still stopping decorative loops and smooth-scroll; `useReducedMotion()` branches added to `ItemCard`/`ChatDock`/`FloatingMenu`/`CaptureBar` so transform/scale springs collapse to opacity-only. Fixed the ChatDock FAB `scale(0)`→`0.9` entrance and replaced its fixed `setTimeout(320)` focus with a `requestAnimationFrame` focus. Updated `docs/ui/theming.md` (Motion Tokens + Reduced Motion sections). Typecheck + build green; lint clean on changed files. Remaining motion tasks tracked in the audit task list.
- [2026-07-14] Added `docs/plan/2026-07-14-uiux-motion-audit-task-list.md`, the execution backlog derived from the UI/UX motion audit. Groups the findings into P0 (motion token layer, reduced-motion rebuild, always-on blur), P1 (canvas pan compositor rewrite, `scale(0)` fix, press-feedback system, framer transform strings, `transition: all`, overlong image transition), P2 (Tooltip interruptibility/origin, FAB float + focus timing, transparency/contrast + touch-hover gating, dead keyframe), and P3 additive delight (feed `AnimatePresence`/`layout`, modal exits, extension state-change motion, chat entrances). Each task carries acceptance checks; a milestone order sequences foundation → accessibility → performance → physicality → delight. No code changed.
- [2026-07-14] Added `docs/plan/2026-07-14-uiux-motion-audit.md`, a UI/UX motion and animation audit spanning the Next.js web app and the WXT Chrome extension. Run as six parallel category subagents against the `improve-animations`/`apple-design`/`emil-design-eng` bars with every citation re-verified. Headline findings: no `--ease-*`/`--duration-*`/spring token layer (motion hand-typed across ~27 files, a duplicated `float` keyframe, five near-duplicate spring configs); reduced-motion is a blanket `!important` clamp that kills helpful fades yet misses all framer-motion springs (`useReducedMotion()` used in zero files); three full-viewport `blur(80px)` Atmosphere blobs plus per-card `backdrop-filter` composite on the feed scroll path; canvas pan animates `left/top`+`background-position` via React state per frame; a `scale(0)` ChatDock entrance; and missing press feedback across pills/extension/cards. Extension is clean on performance. No source code changed — the report is planning-only with a prioritized execution order and doc follow-ups.
- [2026-07-07] Added `docs/plan/2026-07-07-app-performance-security-feature-audit.md`, a current RecallQ app audit covering production dev-bypass risk, reminder plan-limit bypasses, jobs-worker health coverage, synchronous import/export hot paths, feed/sync index gaps, capture-channel byte caps, dependency pinning, environment-doc drift, and client feature enhancement priorities.
- [2026-07-07] Karakeep import/automation/webhook/job split slice. Added Pocket, Omnivore, Linkwarden, and generic CSV import endpoints with dry-run previews, duplicate/session accounting, and parser coverage. Automation rules can now mark items favorite, archived, or read-later. Added outbound webhook subscriptions and deliveries (migration 043) with encrypted signing secrets, masked responses, HMAC signatures, retries, and item created/updated/deleted/enriched event enqueue hooks. The generic jobs worker now claims import, archive, and webhook jobs plus RSS scheduling and archive-retention sweeps, leaving enrichment and reminders isolated. Updated database, route, capture, deployment, testing, and task-list docs.
- [2026-07-07] Karakeep ecosystem/productization slice. Added public collection share links (migration 046), plan-gated custom enrichment prompts (migration 047), bearer-enabled chat for agent clients, richer `pnpm recall` CLI token/file/admin flows, a stdio MCP server, mobile `.well-known` app-link placeholders, a minimal Helm chart, SDK/MCP docs, and an i18n planning doc.
- [2026-07-07] Karakeep archive durability completion. Added migration 041 for archive retention/deleted metadata and video asset-kind stubs. Archive requests can specify asset kinds (`html`, `screenshot`, `pdf`, `video`); the worker stores Playwright-rendered screenshot/PDF assets when a renderer is installed, otherwise records explicit unsupported failed asset rows. The jobs worker now runs hourly archive-retention sweeps, `scripts/admin-jobs.mjs archive-retention --apply` provides dry-run-first cleanup, and archive docs/task-list/testing notes were refreshed.
- [2026-07-07] Karakeep reader/review slice. Added migrations 036 and 037 for item reading state, favorite/archive/read-later flags, link-review metadata, and `item_highlights`. New item APIs expose reader text from archived extracted text with fallbacks, reading-state updates, highlight CRUD/search, and broken-link review actions. Feed filters now include reader/review states, item detail renders reader text plus highlights/progress controls, and search matches highlight quote/note text. Added `apps/web/tests/reader-state.test.ts` and refreshed database/API/search/testing/task-list docs.
- [2026-07-07] Karakeep sync/mobile follow-up. Bearer `PATCH /api/v1/items/[id]` is used by mobile item edits, `GET /api/v1/collections` is bearer-readable for folder pickers, and `@recall/api-client` exposes collection listing plus URL archive requests. Mobile item detail can edit title, note, tags, folder, reminder, and queue archive jobs. Extension sync preserves folder, reminder, archive, and link-health metadata. Sync conflict handling is documented as server timestamp last-write-wins across web, extension, and mobile.
- [2026-07-07] Karakeep task-list execution wave 3. Added archive opt-in/status APIs, sanitized HTML/text archive capture, archive asset cleanup on delete, and link-health item fields (migration 027). Added browser/Netscape bookmark import with import sessions, JSON export, and Netscape bookmark export. Added advanced search grammar/cursors, feed search load-more support, and backend saved-search CRUD (migration 032). Added RSS feed subscriptions, idempotent feed-entry ingestion through the generic jobs worker, `source = 'rss'`, and event-aware automation rules for capture/import/RSS (migrations 034-035). Updated route/database/data-flow/capture/search/testing/deployment docs and task-list statuses.
- [2026-07-07] Karakeep operations/ecosystem slice. Added migration 039 `query_embedding_cache`, migration 040 `operation_logs`, query embedding cache helpers wired into search/chat query embeddings, enrichment/job operation logging, safe admin/backfill scripts, a deterministic OpenAPI generator plus `docs/openapi.json`, a small bearer-token CLI, and Docker Compose self-host files with supporting infrastructure docs.
- [2026-07-07] Karakeep task-list execution wave 2. Added migration 020 and `apps/web/lib/enrichment-claim.ts` so the enrichment worker now claims work with `FOR UPDATE SKIP LOCKED`, retry backoff, stale-lock reclaim, attempt counters, and terminal failure status; added focused worker-claim tests. Added ingest limit regression tests for free/starter/pro/self-hosted save, storage, and reminder branches. Added migration 021 `item_tombstones`, bearer `PATCH /api/v1/items/[id]`, tombstone-aware delta sync, extension edit push for title/note/tags, and extension delete pull reconciliation. Added Expo dev-build setup (`apps/mobile/eas.json`, `start:dev-client`, README checklist). Added migration 022 `items.url_host` with `(user_id, url_host)` index and moved same-domain relation lookup off raw URL string parsing. Added schema foundations: migration 023 generic `jobs`, migration 024 import sessions, migration 025 automation rules, and migration 026 archive assets. Updated the Karakeep task list note/statuses and refreshed API/database/data-flow/testing/extension/performance/capture docs.
- [2026-07-07] Added `docs/plan/2026-07-07-karakeep-audit-task-list.md`, a detailed execution backlog derived from the Karakeep comparison audit. It references the source report, tracks completed first-wave P0 work, and enumerates remaining correctness, archive durability, reader, import/export, search, sync, automation, operations, ecosystem, and documentation tasks with acceptance checks.
- [2026-07-07] Karakeep audit implementation wave 1. Added SSRF-safe remote fetching in `apps/web/lib/url-safety.ts` and routed enrichment URL scraping plus blur-image fetching through it. Hardened `apps/web/lib/ingest.ts`: capture now resolves inferred folders inside the ingest transaction, locks the user row, checks finite save/storage/reminder caps without passing `Infinity` into SQL, commits item insert + save count + storage accounting + reminder insert together, and deletes files if the DB transaction fails. `apps/web/lib/storage.ts` no longer increments storage on file write, fixing double-counted file uploads. Added migration 018 (`idx_items_pending_enrichment`) and migration 019 to allow `items.source = 'mobile'`; web validation and ingest types now accept mobile captures. `apps/mobile` received Expo Go fallbacks and `apps/mobile/README.md`: LAN API derivation from Metro host, guarded notification handling, push registration skipped in Expo Go, Apple availability checks, and removed missing splash/icon asset references. Added URL safety tests and refreshed capture/data-flow/API/security/performance/testing/environment docs.
- [2026-06-16] Chrome extension feature build-out — **local-first** model. The extension (`apps/extension/`) becomes a tab-management + capture suite where **all users save unlimited tabs/URLs into an on-device archive** (`lib/local-archive.ts`, `chrome.storage.local` + `unlimitedStorage`), free, signed-out and offline; the **cloud** (server archive + cross-device) is the **paid** layer. **Context menu** (`entrypoints/background.ts`) rebuilt as a parent "RecallQ" menu with bulk **offload-and-clear** tab operations (this tab / window / except-this / selected / tab-group / left / right / all-windows) — each saves locally then closes the tabs; "Send all tabs in this window" resets the window to one blank tab. Per-tab session **exclusion** (`lib/exclusion.ts`) with a dynamic Exclude/Include menu title. **Pinned-tab app** (`entrypoints/app/` → `app.html`, deduped via `openOrFocusApp`) — Feed reads the local archive (live via `storage.onChanged`, in-memory search), inline capture, Settings. **Two-way cloud sync** (`lib/sync.ts`, gated by `canUseCloudSync`): push dirty → `ingest.batch`; pull → `GET /items?since=` delta; dedup by serverId/URL; backfills on enable; respects server plan caps (surfaces "N pending"). New manifest permissions `tabs`, `tabGroups`, `unlimitedStorage` (no content scripts — zero per-page memory). `packages/api-client` gained `ingest.batch`, `me.get`, `items.list({ since })`, `items.delete`, `ListItem.updated_at`. Server: `GET /items` gained a `?since=` delta branch; `GET /items`, `GET /me`, `DELETE /items/:id` widened to `requireUser` (bearer); plan entitlement `canUseCloudSync`. New `docs/modules/extension.md`. Known v1 cuts (logged): server→local delete propagation + edit re-push deferred. Background bundle stays React-free; total build ~232 KB.
- [2026-05-22] Performance + security audit pass. Six fixes applied inline: (1) OAuth-to-PAT account linking is now gated on the provider's `emailVerified` claim in `apps/web/lib/oauth-link.ts` — closes a defence-in-depth gap where a future provider returning unverified emails could enable takeover of an existing password-based account. (2) New migration 017 adds `idx_accounts_provider_lookup` on `(provider, "providerAccountId")` and `idx_accounts_user_id` on `"userId"` — every OAuth sign-in was previously doing a sequential scan on `accounts`. (3) Rate limit added to `POST /api/v1/payments/stripe/checkout` (5/user/hour). (4) Rate limit added to `POST /api/v1/payments/stripe/portal` (10/user/hour). (5) Rate limit added to `POST /api/v1/devices/push` (60/user/hour). (6) `/api/v1/items` `limit` query param now `Number.isFinite`-guarded and clamped to `[1, 50]` — previously a non-numeric value would propagate `NaN` into the SQL `LIMIT` clause. Audits live in the updated `docs/security-audit.md` (now covers all Stage 8/9-era routes including `oauth/token`, `devices/push`, `payments/stripe/*`) and the new `docs/performance-audit.md` (full migration-001-to-017 index inventory, query patterns, worker cost, operational growth). Four P1 follow-ups documented but deferred: NextAuth credentials rate limit, `ChatRequestSchema` Zod validation, `/api/v1/actions/preview` rate limit, ingest bucket consolidation across `items POST + ingest + ingest/file`.
- [2026-05-22] Dashboard polish pass (round 2). **Sticky filter bar**: the Feed filter row now sticks at `top-16` (under the AppShell header) with backdrop-blur, so all controls stay reachable while scrolling long archives. **Keyboard shortcuts**: new `apps/web/lib/use-feed-keyboard.ts` hook drives j/k (or arrow keys) for card-by-card navigation, `Enter`/`e` opens the detail modal, `r` sets a "tomorrow 9 AM" reminder on the focused card, `c` opens Capture, `?` toggles a help overlay listing all shortcuts. Keyboard navigation pauses when the user is typing in an input/textarea. The focused card gets a subtle brand ring; mouse hover deactivates keyboard focus so the ring doesn't linger. **Modal lifted to the Feed**: `ItemDetailModal` is now rendered once per page (instead of one per card), driven by both mouse clicks and the `e` shortcut. **Item detail modal redesign**: image-led header at 2.2:1 aspect with overlay pills, inline-editable title (was a labeled input), folder picker shrunk and pointed at the new Settings → Folders page, save button disabled until the form is dirty. Mobile collapses to single-column scroll inside the modal. **Onboarding refresh**: `OnboardingBanner` matches the new visual language — gradient hero card with capture CTA + keyboard-shortcut hint, 4-tile grid for the actual capture surfaces (Quick capture, Chrome extension, iOS/Android, Telegram/email), each linking to where the user can set them up. Dropped the stale "knowledge graph" tile since that view was cut in Stage 3.
- [2026-05-22] Dashboard UI/UX refresh. `apps/web/components/ItemCard.tsx` rewritten as a single image-led layout (dropped the list/grid `view` prop entirely): preview image leads at 16:9 with a gradient overlay carrying the type + host pills, body sits below with `text-base` title + 3-line summary, footer with date + `Enriching…` indicator, hover actions float as icon buttons in the card's top-right corner. `apps/web/components/FeedPageClient.tsx` rewrote from 1080 → ~650 lines: dropped the All/Recent/Links/Reminders preset pills (redundant with filter dropdowns), dropped the list/grid toggle, dropped the right sidebar's folder customizer (rename / icon / color) entirely. Single column at `max-w-3xl`. Filter row is one bar with Type / Source / Folder / Sort dropdowns, a `+ Folder` quick-create, and the Select toggle. Active filters render as removable chips below the bar. Tag rail shows top 8 with `+N more` expansion. New `/app/settings/folders` page (added to SettingsNav between Profile and Integrations) owns full folder management — list with inline edit-in-place for name + icon + color, save on dirty, delete confirmation. The inline `+ Folder` shortcut in the feed only creates; everything else moved to Settings.
- [2026-05-22] OAuth (Apple + Google), Expo Push notifications, and a mobile offline capture queue all landed in one pass. **Web**: Apple provider added to NextAuth via `next-auth/providers/apple`; client secret JWT signed at module load with `lib/apple-secret.ts` (ES256, ~5-month TTL). Login page now hides each OAuth button unless its env is configured. **Mobile OAuth**: new `lib/oauth.ts` (Apple via `expo-apple-authentication`, Google via `expo-auth-session`) posts the verified ID token to the new `POST /api/v1/auth/oauth/token` endpoint, which validates against the provider's JWKS (`lib/oauth-verify.ts` using `jose`), finds-or-creates the user with a linked `accounts` row, and mints a PAT. **Push**: migration 016 adds `device_push_tokens`; new `lib/expo-push.ts` (batched up to 100, auto-revokes `DeviceNotRegistered`); `POST /api/v1/devices/push` register endpoint accepts both session and bearer auth; reminder worker fans out the `push` channel to BOTH web-push and Expo Push. Mobile registers its token via `lib/push.ts` after sign-in + on every cold boot. **Offline queue**: mobile `lib/offline-queue.ts` (Expo SQLite) catches failed captures; sync drain runs on sign-in + AppState `active` transition; Feed renders local pending rows alongside server items with a `Pending sync` badge. Mobile root layout wires a notification-tap deep link to `/item/[id]`. Bumped web `tsconfig` target to ES2022 (needed for top-level await of the Apple secret). Added `jose` as a direct web dep.
- [2026-05-22] Stripe billing added alongside Razorpay. Migration 015 adds `stripe_customer_id`, `stripe_subscription_id`, `stripe_price_id`, and a `billing_provider` discriminator column. New `apps/web/lib/billing-stripe.ts` holds Stripe-specific helpers (parallel to the existing `lib/billing.ts` Razorpay helpers rather than forced into a single interface — each provider's API surface is different enough that an over-abstraction would hide more than it shares). New endpoints `/api/v1/payments/stripe/checkout` (Checkout Session) and `/api/v1/payments/stripe/webhook` (signature-verified, handles `checkout.session.completed` + `customer.subscription.*` + `invoice.payment_failed`). `/api/v1/payments/cancel-subscription` is now provider-aware via `users.billing_provider`. Six new env vars (`STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_STARTER_YEARLY_ID`, `STRIPE_PRICE_PRO_YEARLY_ID`). Worker fix: `Resend` constructor is now lazy in `reminder-worker.ts` so the worker boots even without `RESEND_API_KEY` (email channel skipped with a warn).
- [2026-05-22] Stage 9 (foundation): Expo mobile app (`apps/mobile/`) scaffolded with Expo SDK 52, React Native 0.76, Expo Router 4, NativeWind 4. Bundle ID `ai.montr.recallq`, scheme `recallq://`. Screens: `(auth)/sign-in` (email+password → `/api/v1/auth/tokens` via the typed `@recall/api-client`), `(tabs)/index` (feed with pull-to-refresh), `(tabs)/capture` (URL/text → ingest), `(tabs)/settings` (account info + sign out), `item/[id]` (detail with open-link). Token persistence via Expo SecureStore. Shared `AuthProvider` gates the tabs and redirects to sign-in. `app.json` carries Android intent filters (SEND + recallq.xyz path deep links) and iOS `associatedDomains` for future universal-link wiring. `@recall/api-client` got `items.list` + `items.get`. Monorepo React types aligned via `pnpm-workspace.yaml` overrides (`@types/react: 18.3.12`, `@types/react-dom: 18.3.5`) — web on React 19 runtime works with @types/react 18.3 (the Next-recommended pattern), and RN/Expo's types are compiled against the same. **Deferred to v1.5**: Share Extension (iOS) / share intent handling, Expo Push notifications + device registration, offline capture queue via Expo SQLite, Google + Apple OAuth, camera/document picker, universal/app link content handlers, EAS build setup, splash + icon assets.
- [2026-05-22] Stage 8: Chrome extension (`apps/extension/`) scaffolded via WXT 0.20 — Manifest V3, React popup, background context menu (`Save link to RecallQ`, `Save selection to RecallQ`), token persisted in `chrome.storage.local`. Auth via `chrome.identity.launchWebAuthFlow` → new web bridge `apps/web/app/extension/connect/page.tsx` that requires NextAuth session, mints a personal access token, and redirects back to the extension's reserved `chromiumapp.org` URL. New `packages/api-client` (typed REST client, `createRecallClient`) consumed by the extension; web app keeps hitting `/api/v1/*` same-origin without it. `apps/web/proxy.ts` middleware extended to handle CORS preflight + headers for `/api/v1/*` (permissive because the API is bearer-token-gated). Root scripts added: `pnpm build:ext`, `pnpm build:all`, `pnpm zip:ext`, `pnpm dev:ext`. Extension build is ~205 KB total (popup + background + manifest + CSS).
- [2026-05-22] Stage 7: Visual polish + a11y. Sitewide `:focus-visible` outline added in `apps/web/app/globals.css` so every interactive element shows a brand-colored focus ring under keyboard navigation (suppressed under mouse). New `apps/web/lib/use-modal-a11y.ts` shared hook handles body scroll lock, auto-focus on open, focus restoration on close, and Tab/Shift+Tab focus trap — adopted by both `CreateItemDialog` and `ItemDetailModal`. `FeedPageClient` gets a real search-no-results state: search icon, the query echoed in a heading, and a "Clear search" button that returns to the full archive.
- [2026-05-22] Stage 6: Performance work. Removed the prior force-directed graph package after the Stage 3 canvas rewrite. Lazy-loaded `@xyflow/react` so the canvas bundle only ships on `/app/canvas` and Feed, ChatDock, Settings, and auth pages skip it. New migration 014 adds `items.blur_data_url`; the enrichment worker now computes a 16x16 base64 JPEG placeholder via `apps/web/lib/blur.ts` (sharp) whenever it scrapes an `image_url`, and `ItemCard` renders it through `next/image` with `placeholder="blur"` to eliminate CLS on the feed. `/api/v1/health` gets a 10s edge cache so uptime probes don't ping the DB on every poll. A11y nibbles: `aria-label` + `aria-expanded` on the sidebar collapse button, `aria-label` on the per-card delete button. Sharp added to `apps/web/package.json` so workers and (future) image-resize routes can use the same library.
- [2026-05-22] Stage 5: Security hardening. Migration 013 adds `rate_limits`; new `apps/web/lib/rate-limit.ts` is an atomic Postgres-backed fixed-window limiter (no Redis). Applied to `POST /api/v1/auth/tokens` (5/IP/15min + 10/email/hour), `POST /api/v1/chat` (30/user/hour), `POST /api/v1/ingest` (60/user/min). `apps/web/next.config.mjs` now sets HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy on every response, plus a Content-Security-Policy in Report-Only mode (allowlist covers Razorpay, Gemini). `apps/web/lib/env.ts` boot check refuses to start in production with an AUTH_SECRET shorter than 32 chars or with a localhost DATABASE_URL when `SELF_HOSTED!=true` — phase-aware so it doesn't fire during `next build`. `.gitignore` now blocks all `.env*` files except `.env.example`. Per-route audit lives in `docs/security-audit.md` (33 endpoints catalogued, 7 follow-ups deferred to Stage 6/8).
- [2026-05-22] Stage 4: Wired production-grade error handling and observability. `apps/web/lib/logger.ts` now emits JSON-per-line in production (CloudPanel-parseable) and pretty text in dev, gated by the new `LOG_LEVEL` env. Migration 012 adds `worker_heartbeats`; both background workers upsert their row every 30 seconds via the new `apps/web/lib/worker-heartbeat.ts` helper and install `unhandledRejection` / `uncaughtException` handlers that log and exit non-zero so systemd restarts cleanly. New unauthenticated `GET /api/v1/health` endpoint pings the DB and reads heartbeats, returning 200/503 plus per-worker `up`/`down` status. App shell now ships its own `error.tsx`, `loading.tsx`, plus per-page loading skeletons for canvas and chat; `apps/web/app/not-found.tsx` and `apps/web/app/global-error.tsx` are branded last-resort boundaries.
- [2026-05-22] Stage 3: Wired search into the Feed and reshaped Canvas as an infinite freeform board. Extracted `runSearch` / `runHybridSearch` into `apps/web/lib/search.ts` and made `apps/web/app/(app)/app/page.tsx` call it server-side when `searchParams.q` is present. `FeedPageClient` accepts a `searchQuery` prop and renders a clear-pill at the top in search mode. The canvas keeps pan/zoom/drag/persist, `ItemDetailModal` click-through, and archive-event listeners while dropping type swimlanes, in-canvas search, minimap controls, filters, and image-vs-text height variants. New items captured anywhere in the app immediately persist a viewport-center position so they appear where the user is looking. Bottom-right floating dock has Capture, Fit-to-viewport, Refresh. The retrieval module now lives at `docs/modules/search-chat-canvas.md`. Settings audit kept the Appearance subpage.
- [2026-05-22] Stage 2: Deleted duplicate and feature-cut routes, moved settings client files into the canonical `apps/web/app/(app)/app/settings/<sub>/` tree, and added 301 redirects in `apps/web/next.config.mjs` so external bookmarks, search-engine indexes, and the PWA Web Share Target keep working. Canonical authenticated surfaces are now Feed, Canvas, Settings, and the global ChatDock.
- [2026-05-22] Stage 1: Versioned the application API under `/api/v1/*` (15 route directories moved; NextAuth stays at `/api/auth/*`). Added a transparent rewrite in `apps/web/next.config.mjs` so legacy paths keep working through the migration. Introduced `packages/api-schema` (shared Zod schemas, consumed via `transpilePackages`). Added migration 011_personal_access_tokens.sql for bearer-token auth, `apps/web/lib/auth-tokens.ts` for token generation + hashing, `apps/web/lib/api-response.ts` (`ok` / `fail` / `parseBody` helpers), and the three token endpoints (`POST /api/v1/auth/tokens`, `GET /api/v1/auth/tokens`, `DELETE /api/v1/auth/tokens/:id`). `requireUser(req)` in `apps/web/lib/request-auth.ts` now accepts both NextAuth session cookies and `Authorization: Bearer …` headers, ready for the Chrome extension and the mobile apps to consume.
- [2026-05-22] Stage 0: Restructured into pnpm + Turborepo monorepo with the web app under `apps/web/`. Workers moved to `apps/web/workers/`. Migrations, scripts, and docs stay at the workspace root. Single `.env` at root, loaded by `apps/web/next.config.mjs` for build/dev and via Node `--env-file` for workers. ESLint pinned to ^9 (10.x has incompatible plugin breakage). `react-hooks/set-state-in-effect` downgraded to warn pending Stage 3 cleanup. See [PLAN.md](file:///e:/Projects/recallQ/PLAN.md).
- [2026-05-18] Split authentication into dedicated login, signup, forgot-password, and reset-password pages with local password reset tokens.
- [2026-05-17] Switched UI to dark-first default, added quick-capture to shell, and added mobile drawer navigation.
- [2026-05-17] Built hybrid search retrieval page, threaded chat with citation drill-in, and bulk archive/undo actions.
- [2026-05-17] Standardized settings routing and created initial system documentation.

## Update Triggers
- When any documentation file is added, removed, or renamed.
- When there is a major change to the tech stack (e.g. upgrading Next.js version).
- When a new core feature is introduced that changes the high-level project vision.

## Related Docs
- [docs/architecture/folder-structure.md](file:///e:/Projects/recallQ/docs/architecture/folder-structure.md) — Renders the codebase layout described in the tech stack.

AGENT NOTE: overview.md is the first file any agent reads. If it drifts from reality, all downstream decisions will be wrong. Update it whenever any doc file is added, removed, or significantly restructured.
AGENT UPDATE: docs/overview.md
